<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="setting"
            type="com.jainam.story2.player.playerSettings.playerSettingsNavigation.Setting" />
        <variable
            name="clickListener"
            type="com.jainam.story2.player.playerSettings.playerSettingsNavigation.PlayerSettingsNavigationAdapter.SettingClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:layout_marginVertical="10dp"
        android:onClick="@{() -> clickListener.onClick(setting)}"
        android:background="@drawable/ic_rectangle">

        <TextView
            android:id="@+id/settingsTextView"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:lines="1"
            settingName="@{setting}"
            android:singleLine="true"
            android:textColor="@color/colorPrimary"
            app:autoSizeMaxTextSize="14sp"
            app:autoSizeMinTextSize="8sp"
            app:autoSizeTextType="uniform"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@+id/fileTypeImageView"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/fileTypeImageView"
            android:layout_width="33dp"
            android:layout_height="50dp"
            android:layout_marginStart="16dp"
            tools:src="@drawable/ic_voice_icon"
            app:settingIcon="@{setting}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="32dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_next_arrow"
            android:contentDescription="@string/go_to_this_setting_s_page" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
